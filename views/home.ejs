<%- include('partials/header') %>

<h1>Home</h1>

<p><%= homeStartingContent %> </p>

<%# for (let i=0; i<posts.length; i++) {   
    
     console.log(posts[i].title) 
    
 }  En vez del for loop ahora existe el metodo forEach para arrays.%> 



<% posts.forEach(function(post) { %>
    <h1><%= post.title %> </h1>
    <p><%= post.body.substr(0, 100) + "..." %> 
         <a href="/posts/<%= post.title %>" >Read More</a>
        <% });  %>  
    </p>        <%# array.forEach(parametro funcion que por convencion es el singular del contenido del array => que se debe hacer(en este caso, loguear el title de cada post)) %> 


<%- include('partials/footer') %> 

